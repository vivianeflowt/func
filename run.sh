#!/bin/bash



pnpm add -D typescript ts-node typesync @types/node ts-toolbelt type-fest tslib

pnpm add -D @types/eslint @types/eslint-plugin-prettier @types/prettier @typescript-eslint/eslint-plugin 
pnpm add -D @typescript-eslint/parser eslint eslint-config-prettier eslint-config-xo eslint-plugin-jest
pnpm add -D eslint-plugin-prettier eslint-plugin-simple-import-sort prettier

pnpm add -D jest @types/jest ts-jest

pnpm add -D release-it auto-changelog 

pnpm add -D commitizen cz-conventional-changelog lint-staged

pnpm add -D syncpack  rimraf @types/rimraf @faker-js/faker 

pnpm add -D typedoc


pnpm add lodash
pnpm add -D @types/lodash
  
# pnpm add cosmiconfig bluebird fs-extra core.js
